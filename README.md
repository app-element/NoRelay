# NoRelay
Every connection in NoRelay is end-to-end encrypted, identity-verified, and established directly between peers using modern cryptography and NAT traversal. Messages never pass through third-party infrastructure, and no metadata or history is stored beyond a minimal local buffer.
